# RAuth SDK Environment Variables Example
# Copy this file to .env.local and fill in your values

# Development (Local Backend)
# Uncomment these lines when developing with a local RAuth backend
# NEXT_PUBLIC_RAUTH_API_KEY=your_development_api_key
# NEXT_PUBLIC_RAUTH_BASE_URL=http://localhost:8080

# Production
# Use only the API key (defaults to production URL)
# NEXT_PUBLIC_RAUTH_API_KEY=your_production_api_key

# Note: NEXT_PUBLIC_ prefix makes variables available in the browser
# For other frameworks (Vite, CRA), use appropriate prefixes:
# - Vite: VITE_
# - Create React App: REACT_APP_
